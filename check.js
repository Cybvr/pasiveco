const admin = require("firebase-admin");
const serviceAccount = require("./service-account-key.json"); // wait, I don't know where their creds are.

// Actually, in lib/firebase-admin.ts it initializes.
